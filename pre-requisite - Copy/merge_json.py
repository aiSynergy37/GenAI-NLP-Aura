"""
You have 5 files: events_part1.jsonl, events_part2.jsonl, etc.
Each line is a JSON object with at least an "id" field.
Task:

Merge all into one merged_events.jsonl
If same id appears → keep the one with latest "timestamp"
Sort final output by timestamp ascending

"""